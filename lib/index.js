'use strict';

/**
 * Module dependencies.
 */

var request = require('request');

function Api(opt) {
	var self = this;	
	var options = opt || {};

	

	return {
	};
};

/**
 * Export `AnyFile`.
 */

module.exports = Api;